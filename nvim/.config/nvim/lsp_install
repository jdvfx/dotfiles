// install latest neovim version on ubuntu
sudo add-apt-repository ppa:neovim-ppa/unstable
sudo apt-get update
sudo apt-get install neovim


// install pip
sudo apt install python3-pip
// install Python-Language-Server
pip install -U jedi-language-server

// install lua-language server
// download the linux binary
https://github.com/sumneko/lua-language-server/releases

extract and place in /usr/lib/node_modules/lua-language-server

// create an alias to the binary in /usr/bin:
sudo ln -s /usr/lib/node_modules/lua-language-server/bin/lua-language-server /usr/bin/lua-language-server

needs to be neoVim v6.0.0 for telescope to work

// update the neoVim plugins:
inside neoVim, :PackerSync





